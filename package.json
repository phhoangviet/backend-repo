{
  "name": "ebuddy-backend",
  "version": "1.0.0",
  "description": "",
  "main": "core/app.ts",
  "scripts": {
    "dev": "nodemon --exec ts-node ./core/app.ts",
    "start": "node ./dist/core/app.js",
    "copy": "cp -r common config controller core entities middleware repository routes functions/src",
    "build": "npm run copy && tsc -p functions/tsconfig.json",
    "deploy": "npm run build && firebase emulators:start --only functions",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "phhviet",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase": "^10.12.4",
    "firebase-admin": "^12.2.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/logger": "^0.0.5",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}